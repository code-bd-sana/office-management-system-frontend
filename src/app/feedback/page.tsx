import { MainLayout } from "@/components/layout";
import { FeedbackContent } from "../../components/feedback/FeedbackContent";

export default function FeedbackPage() {
  return (
    <MainLayout pageTitle="Feedback">
      <FeedbackContent />
    </MainLayout>
  );
}
